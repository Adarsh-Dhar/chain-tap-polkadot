[relaychain]
# Use the polkadot binary you just built
default_command = "/path/to/your/polkadot-sdk/target/release/polkadot"
chain = "rococo-local" # A built-in local relay spec

[[relaychain.nodes]]
name = "alice"

[[relaychain.nodes]]
name = "bob"

[[parachains]]
id = 1000 # This is the standard testnet ID for Asset Hub

# Use the parachain binary you just built
default_command = "/Users/adarsh/Documents/chainTap/polkadot-sdk/target/release/polkadot-parachain"

# This is the key: tell the binary to run as a local Asset Hub
chain = "asset-hub-rococo-local"

[[parachains.collators]]
name = "collator"